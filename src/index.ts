// @stoachain/ouronet-core — root barrel
//
// Prefer subpath imports (`@stoachain/ouronet-core/guard`) in consumers —
// better tree-shaking than pulling the whole root barrel.
//
// Populated as modules migrate in from OuronetUI over Phases 1–4 of the
// extraction plan (see OuronetUI/docs/EXTRACT_OURONET_CORE_PLAN.md).

export {};
