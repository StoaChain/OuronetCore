// @stoachain/ouronet-core/interactions — Pact builders:
// executeCoil, executeCurl, executeSwap, executeCompress, executeSublimate,
// INFO_* read helpers, coin.C_Transfer wrappers, liquidity builders.
// Populated in Phase 2b when src/kadena/interactions/*.ts moves over.
export {};
